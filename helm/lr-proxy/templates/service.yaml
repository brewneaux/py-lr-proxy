apiVersion: v1
kind: Service
metadata:
  name: {{ include "lr-proxy.fullname" . }}
  labels:
    {{- include "lr-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lr-proxy.selectorLabels" . | nindent 4 }}
